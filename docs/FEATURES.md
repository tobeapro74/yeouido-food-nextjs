# 기능 명세서

## 1. 홈 화면

### 1.1 헤더
- 글래스모피즘 스타일 (`bg-white/80 backdrop-blur-xl`)
- sticky 상단 고정, 스크롤 시 콘텐츠 위에 반투명 표시
- 앱 타이틀: "여의도" (레드 강조) + "한끼"
- 우측 로그인/사용자 메뉴 버튼
  - 비로그인: 사용자 아이콘 (클릭 시 로그인 모달)
  - 로그인: 드롭다운 메뉴 (등록 히스토리, 비밀번호 변경, 로그아웃, 회원탈퇴)

### 1.2 검색바
- 통합 검색 (식당, 빌딩, 음식, 도로명)
- 실시간 자동완성
- 점수 기반 결과 정렬

### 1.3 퀵 카테고리
- 가로 스크롤 카테고리 버튼
- 카테고리: 전체, 한식, 양식, 중식, 일식, 동남아식

### 1.4 인기 맛집
- 카테고리별 최고 평점 맛집 표시
- 가로 스크롤 카드 레이아웃
- 카테고리 배지 표시
- 실시간 Google 평점 연동

### 1.5 지역별 맛집
- 지역 필터 버튼 (서여의도/동여의도)
- 선택된 지역의 맛집 목록
- 평점 높은 순 정렬

### 1.6 하단 네비게이션
- 홈, 카테고리, 빌딩, 한끼추천, 운세맛집 탭
- 관리자/박병철: 맛집등록 탭 추가 표시

## 2. 맛집 목록 화면

### 2.1 헤더
- 뒤로가기 버튼 (44x44px 터치 영역)
- 목록 제목 (예: "한식 맛집", "IFC 맛집")

### 2.2 맛집 카드
- 이미지 (없으면 기본 이미지)
- 맛집명
- 평점 (별표시 + 점수)
- 위치 정보
- 빌딩 배지 (파란색)
- 사용자 등록 맛집 배지

## 3. 맛집 상세 화면

### 3.1 이미지 갤러리
- 메인 이미지 표시
- Google Places 이미지 연동

### 3.2 기본 정보
- 맛집명
- 평점
- 영업 상태
- 주소
- 영업시간
- 빌딩명 배지

### 3.3 추천메뉴
- 메뉴 목록 표시

### 3.4 Google 리뷰 섹션
- Google Places API 연동
- 평점 및 리뷰 수 표시
- 리뷰 내용 미리보기

### 3.5 사용자 리뷰 섹션
- 리뷰 목록 (최신순)
- 리뷰 작성 버튼

### 3.6 외부 링크
- Google Maps 열기
- Google 검색

### 3.7 맛집 정보 수정 (등록자/관리자)
- 카테고리 수정 버튼
- 맛집 정보 수정 버튼 (바텀시트 모달)
- 맛집 삭제 버튼 (등록자만)

### 3.8 데이터 동기화
- **상세 화면 진입 시 DB에서 최신 데이터 자동 fetch**
- 수정 후 즉시 화면 반영 (onUpdate 콜백)
- 뒤로가기 후 재진입해도 최신 데이터 유지

## 4. 회원 인증

### 4.1 회원가입
- 이름 입력
- 이메일 입력 (중복 체크)
- **이메일 인증 코드 발송** (Resend API)
- **6자리 인증 코드 확인** (5분 유효)
- 비밀번호 입력 (bcrypt 암호화)
- 관리자 등록 (관리자 시크릿 키 필요)

### 4.2 로그인
- 이메일/비밀번호 입력
- JWT 토큰 발급 (쿠키 저장)

### 4.3 로그아웃
- 쿠키 삭제

### 4.4 비밀번호 변경
- 현재 비밀번호 확인
- 새 비밀번호 입력

### 4.5 계정 삭제
- 비밀번호 확인 후 계정 영구 삭제
- 관련 리뷰 데이터도 함께 삭제

## 5. 리뷰 기능

### 5.1 리뷰 작성 (바텀시트 모달)
- **바텀시트 UI**: 아래에서 위로 슬라이드, 드래그 핸들 포함
- 전체 별점 (필수, 1~5점)
- 세부 별점 (선택)
  - 음식
  - 서비스
  - 분위기
- 리뷰 내용 (텍스트)
- 사진 첨부 (최대 4개)
  - 클라이언트 리사이즈 (800px, 60% 품질)
  - Cloudinary 업로드
- 식사 유형 선택 (아침/브런치/점심/저녁/기타)

### 5.2 리뷰 목록
- 작성자명
- 별점
- 작성일
- 리뷰 내용
- 사진 (있는 경우)

### 5.3 리뷰 수정
- 본인 리뷰만 수정 가능
- 기존 데이터 자동 로드
- 별점, 내용, 사진, 식사 유형 모두 수정 가능

### 5.4 리뷰 삭제
- 본인 리뷰만 삭제 가능
- 삭제 전 확인 모달 표시

## 6. PWA 지원

### 6.1 매니페스트
- 앱 이름: 여의도한끼
- 테마 색상: #FF6B6B
- 홈 화면 추가 지원

### 6.2 아이콘
- 192x192, 512x512 PNG
- Apple Touch Icon

### 6.3 뷰포트 설정
- 모바일 최적화
- Safe Area 지원 (iOS 노치)

## 7. 기술적 특징

### 7.1 이미지 최적화
- 클라이언트 사이드 리사이즈
- JPEG 압축 (60% 품질)
- Cloudinary CDN 활용

### 7.2 반응형 디자인
- 모바일 퍼스트
- 최대 너비 md (448px) 제한
- 가로 스크롤 영역

### 7.3 성능 최적화
- Next.js App Router
- 정적 데이터 캐싱
- 이미지 lazy loading

## 8. 한끼추천

### 8.1 기분별 추천
- 피곤해, 가볍게, 특별하게, 매콤하게, 국물땡김, 고기고기

### 8.2 룰렛 돌리기
- 카테고리 기반 랜덤 추천
- 6개 섹션: 한식, 양식, 중식, 일식, 동남아, 랜덤
- CSS 애니메이션 룰렛

### 8.3 취향 설정
- 카테고리, 지역, 가격대, 제외 태그 필터

## 9. 운세맛집

### 9.1 입력 정보
- 생년월일, 성별, 결혼여부

### 9.2 오행 계산
- 천간지지 기반 본명 오행 산출 (목/화/토/금/수)
- 띠(12지지) 계산

### 9.3 운세 결과
- 나의 오행 및 띠
- 오늘의 길방 (동여의도/서여의도)
- 추천 음식 카테고리
- 추천 맛집 리스트

### 9.4 운세 지수 시스템
- 종합운, 재물운, 가정운, 사회운 (각 1-5점)
- 터치 시 상세 해설 모달

## 10. 네비게이션 UX

### 10.1 iOS 스타일 스와이프 뒤로가기
- 화면 왼쪽 가장자리에서 스와이프 시작
- 페이지 전체가 오른쪽으로 슬라이드
- 배경 어두워지는 오버레이 효과
- 스와이프 완료 시 페이지 밀어내기 애니메이션
- 스와이프 취소 시 부드러운 복귀

### 10.2 뒤로가기 버튼
- 44x44px 터치 영역 (Apple HIG 권장)
- 반투명 배경으로 가시성 확보
- 둥근 원형 디자인

### 10.3 Safe Area 지원
- iOS 노치/다이나믹 아일랜드 대응
- 헤더 상단 여백 자동 조정

## 11. 사용자 맛집 등록

### 11.1 맛집 검색 (Google Places)
- Google Maps 장소 검색 연동
- 장소명, 주소, 좌표 자동 입력
- 평점, 리뷰 수, 가격대 자동 조회

### 11.2 카테고리 선택
- 한식, 양식, 중식, 일식, 동남아식
- 등록 후 카테고리 수정 가능 (등록자/관리자)

### 11.3 중복 등록 방지
- place_id 기반 중복 체크

### 11.4 등록 권한
- 관리자 또는 특정 사용자만 맛집 등록 가능
- 등록자, 관리자, 또는 박병철만 카테고리 수정 가능
- 등록자, 관리자, 또는 박병철만 삭제 가능

### 11.5 맛집 정보 수정 (바텀시트 모달)
- 등록자, 관리자, 또는 박병철만 수정 가능
- 수정 가능 항목: 주소, 특징/메모, 전화번호, 영업시간, 카테고리
- 수정 전 확인 모달 표시
- 변경사항 히스토리 자동 기록

### 11.6 정적 데이터 자동 마이그레이션
- 정적 데이터(코드에 하드코딩된 맛집) 수정/삭제 시 자동으로 DB로 마이그레이션
- 마이그레이션 후 일반 사용자 등록 맛집과 동일하게 관리
- place_id 형식: `static_${맛집명}_${카테고리}`
- 예시: `static_김삼보_한식`, `static_딘타이펑_중식`
- 관리자 또는 박병철만 정적 데이터 수정/삭제 가능

### 11.7 정적 데이터 삭제 (Soft Delete)
- 정적 데이터 삭제 시 실제 삭제 대신 `deleted: true` 플래그 설정
- DB에 삭제 기록 유지 (복구 가능)
- 목록 조회 시 `deletedStaticIds` 배열로 삭제된 정적 데이터 필터링
- 삭제된 정적 데이터는 카테고리/빌딩 목록에서 자동 제외

## 12. 맛집 등록 히스토리

### 12.1 히스토리 목록
- 로그인 사용자 전용 기능
- 사용자 메뉴에서 "등록 히스토리" 접근
- 테이블 형태로 등록/수정/삭제 내역 표시

### 12.2 표시 정보
- 순번 (seq): 자동 증가 번호
- 날짜: 등록/수정/삭제 일시
- 맛집명: 등록된 맛집 이름 (클릭 시 상세 화면 이동)
- 지역: 간단한 주소 (구/동 단위)
- 카테고리: 맛집 카테고리
- 상태: 등록(녹색)/삭제(빨강)/수정(파랑) 배지

### 12.3 자동 기록
- 맛집 등록 시 자동으로 히스토리 생성
- 맛집 삭제 시 자동으로 히스토리 생성
- 카테고리 수정 시 자동으로 히스토리 생성
- 맛집 정보 수정 시 자동으로 히스토리 생성

### 12.4 페이지네이션
- 페이지당 20개 항목
- 최신순 정렬 (seq 내림차순)
- 이전/다음 페이지 네비게이션

### 12.5 반응형 레이아웃
- **모바일**: 카드 형식 레이아웃
  - 상단: 번호, 상태 배지, 카테고리 / 날짜(우측)
  - 중간: 맛집명 (클릭 시 상세 이동, 애니메이션 효과)
  - 하단: 지역, 메모
- **데스크탑**: 테이블 형식 레이아웃
  - 컬럼: #, 날짜, 맛집명, 지역, 카테고리, 상태
  - overflow-hidden으로 텍스트 겹침 방지

### 12.6 맛집 상세 이동
- 히스토리에서 맛집명 클릭 시 상세 화면으로 이동
- 삭제된 맛집은 클릭 불가 (취소선 표시)
- 상세 화면에서 뒤로가기 시 히스토리로 복귀

## 13. 모달 UX

### 13.1 바텀시트 모달 (맛집수정, 리뷰)
- **애니메이션**: 아래에서 위로 슬라이드 (slide-up, 0.3s)
- **드래그 핸들**: 상단 중앙에 회색 바 표시
- **배경 터치**: 배경 클릭 시 모달 닫힘
- **모서리**: 상단만 둥글게 (`rounded-t-3xl`)
- **최대 높이**: 85~90vh
- **Safe Area**: 하단 네비게이션바 고려 (`pb-[env(safe-area-inset-bottom)]`)

### 13.2 센터 모달 (로그인, 맛집등록, 비밀번호변경 등)
- **위치**: 화면 중앙
- **Safe Area**: 상하단 모두 고려
  - 상단: `pt-[calc(env(safe-area-inset-top)+16px)]`
  - 하단: `pb-[calc(env(safe-area-inset-bottom)+80px)]`
- **최대 높이**: `max-h-[calc(100vh-160px-env(safe-area-inset-top)-env(safe-area-inset-bottom))]`

### 13.3 배경 스크롤 방지
- 모든 모달에서 `document.body.style.overflow = "hidden"` 적용
- 모달이 열려있을 때 배경 스크롤 방지
- 모달 닫힐 때 `overflow` 원복

### 13.4 z-index 체계
- 기본 모달: `z-[100]`
- 확인 모달 (중첩): `z-[110]`
- 회원탈퇴 모달: `z-[200]`

## 14. 디자인 시스템 (2026.02.07)

### 14.1 폰트
- Pretendard Variable (한글) + Geist Sans (라틴)
- CDN: dynamic subset 방식으로 필요한 글리프만 로드

### 14.2 토스트 알림 (sonner)
- `alert()` 대신 비블로킹 토스트 사용
- `toast.success()`: 성공 (삭제 완료, 회원가입 완료 등)
- `toast.error()`: 실패/오류
- `toast.warning()`: 경고 (별점 미선택 등)
- `toast.info()`: 안내 (로그인 필요 등)

### 14.3 터치 영역 (NDS 기준)
- 버튼 기본: 40px (`h-10`)
- 버튼 lg: 44px (`h-11`)
- 아이콘 버튼: 40px (`size-10`)
- 하단 네비게이션: 64px (`h-16`)

### 14.4 애니메이션
- `animate-fade-in`: 0.2s ease-out (모달 오버레이)
- `animate-slide-up`: 0.3s ease-out (바텀시트)
- `animate-scale-in`: 0.2s ease-out (다이얼로그)
- `animate-fade-in-up`: 0.4s ease-out (리스트 항목)

### 14.5 빈 화면 (EmptyState)
- 공통 컴포넌트: `src/components/ui/empty-state.tsx`
- 아이콘 + 제목 + 설명 + 액션 버튼
- 검색 결과 없음 등에 적용
